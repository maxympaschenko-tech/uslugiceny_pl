# Apache: przekierowania, kompresja, cache, strona 404.
# Generator kopiuje ten plik do katalogu glownego dist/.

Options -Indexes
DirectoryIndex index.html

ErrorDocument 404 /404.html

# Wymuszenie HTTPS.
# Uwaga: hosting konczy SSL na proxy, wiec Apache widzi zwykly HTTP.
# Sam warunek %{HTTPS} !=on dawalby petle przekierowan, dlatego sprawdzamy
# dodatkowo naglowek X-Forwarded-Proto: przekierowujemy tylko wtedy,
# gdy zadne ze zrodel nie mowi, ze polaczenie jest szyfrowane.
<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteCond %{HTTPS} !=on
  RewriteCond %{HTTP:X-Forwarded-Proto} !=https
  RewriteCond %{HTTP:X-Forwarded-SSL} !=on
  RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

  RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
  RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml text/xml
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  # Arkusz stylow ma w adresie odcisk tresci (?v=hash), wiec roczny cache jest
  # bezpieczny: zmiana pliku zmienia adres. Ikony i obrazki tak samo.
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/png "access plus 1 month"
  ExpiresByType image/x-icon "access plus 1 month"
  # Strony i dane wyszukiwarki musza sie odswiezac czesto, bo zmieniaja sie
  # razem z cennikiem.
  ExpiresByType text/html "access plus 1 hour"
  ExpiresByType application/json "access plus 1 hour"
  ExpiresByType application/xml "access plus 1 day"
</IfModule>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>
